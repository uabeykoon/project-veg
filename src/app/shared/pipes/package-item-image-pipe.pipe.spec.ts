import { PackageItemImagePipePipe } from './package-item-image-pipe.pipe';

describe('PackageItemImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PackageItemImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
