import { CartQuantityAndWeightPipePipe } from './cart-quantity-and-weight-pipe.pipe';

describe('CartQuantityAndWeightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CartQuantityAndWeightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
