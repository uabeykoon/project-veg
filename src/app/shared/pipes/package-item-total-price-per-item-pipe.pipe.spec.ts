import { PackageItemTotalPricePerItemPipePipe } from './package-item-total-price-per-item-pipe.pipe';

describe('PackageItemTotalPricePerItemPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PackageItemTotalPricePerItemPipePipe();
    expect(pipe).toBeTruthy();
  });
});
