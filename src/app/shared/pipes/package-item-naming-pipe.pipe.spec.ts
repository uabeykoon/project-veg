import { PackageItemNamingPipePipe } from './package-item-naming-pipe.pipe';

describe('PackageItemNamingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PackageItemNamingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
