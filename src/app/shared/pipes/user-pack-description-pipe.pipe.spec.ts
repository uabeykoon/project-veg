import { UserPackDescriptionPipePipe } from './user-pack-description-pipe.pipe';

describe('UserPackDescriptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserPackDescriptionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
